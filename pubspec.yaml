name: asset_span_builder
description: A script for building asset spans from input files.
version: 0.0.1
repository: https://github.com/chrisnorman7/asset_span_builder
environment:
  sdk: ^3.7.0

dependencies:
  json_annotation: ^4.9.0
  path: ^1.8.0
  recase: ^4.1.0
  yaml: ^3.1.3
  yaml_edit: ^2.2.2

dev_dependencies:
  build_runner: ^2.4.15
  json_serializable: ^6.9.5
  lints: ^6.0.0
  test: ^1.24.0

executables:
  asb:
